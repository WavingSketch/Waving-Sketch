#ifndef DEFINITION_H
#define DEFINITION_H

/*
 * define frequently-used variable and function
 */

typedef unsigned int uint;
typedef unsigned char uchar;
typedef unsigned long int ulong;

#define MAX(a, b) (a > b? a:b)
#define MIN(a, b) (a < b? a:b)

#endif // DEFINITION_H
